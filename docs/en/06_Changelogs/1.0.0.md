---
Title: Release 1.0.0
Date: 2025-07-22 07:00
Category: changelog
Summary: This is major release that marks the first full release of Silverstripe Search. It has some notable changes to [feature availability](#feature-changes) and some API changes.
---

This is major release that marks the first full release of Silverstripe Search. It has some notable changes to feature availability and some API changes.

## Feature Changes

Elastic Enterprise Search has been removed from Silverstripe Search as part of ongoing work to replace its deprecated functionality and ensure security compliance. Features will be gradually replaced as they are ready with Synonyms being available via the new [Silverstripe Search Administration module](#silverstripe-search-administration-module).

Login to the Enterprise search dashboards is no longer available. This affects the following features:

1. Synonyms (available in the [Silverstripe Search Administration module](#silverstripe-search-administration-module))
1. Analytics
1. Curations
1. Relevance tuning (AKA weights and boosts)
1. Log viewing

Synonyms are available via the new [Silverstripe Search Administration module](#silverstripe-search-administration-module).

Weights are available in the [API](/api/v1/docs/#/search/search_post) and SDK modules. For example the [Discoverer module documentation](https://github.com/silverstripeltd/silverstripe-discoverer/blob/main/docs/detailed-querying.md#search-fields) shows how to apply weights when making a search.

Other features will not be available until they can be released in later versions.

## API changes

As part of the feature changes some API endpoints have been added and some removed.

### Added endpoints

-   [Synonym management](/api/v1/docs/#/synonyms)
-   [Settings management](/api/v1/docs/#/settings)

### Removed endpoints

-   Analytics click endpoint: `/{engine_name}/click`

The analytics click endpoint is designed to be used via the [discoverer module](https://github.com/silverstripeltd/silverstripe-discoverer) with our [Bifröst provider](https://github.com/silverstripeltd/silverstripe-discoverer-bifrost) and it will ignore any calls to this endpoint to prevent sites from breaking after the release.

### Other changes

-   Search endpoint([/api/v1/docs/#/search/](/api/v1/docs/#/search/)) is now stricter and will not allow fields that are not part of the specification. There are no changes for valid queries.

## Silverstripe Search Administration module

In order to make dashboard features available to Silverstripe Search customers quickly a new Administration module is provided with this release. It can be installed on any Silverstripe site compatible with Silverstripe Search (version 5 at the time of release). This release includes support for [Synonym management](/features/relevancy/synonyms) and viewing your engine's [Schema](/features/engines-and-schema#schema).

It can be installed with composer `composer require silverstripeltd/silverstripe-bifrost-admin`. It depends on the `silverstripe/silverstripe-forager` module to provide credentials and does not require any user login beyond the CMS. Permission to access the dashboard can be configured in the CMS. For all other details please refer to [the module's documentation](https://github.com/silverstripeltd/silverstripe-bifrost-admin).
