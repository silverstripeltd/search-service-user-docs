name: Build & Deploy Docs
on:
  workflow_dispatch:
  push:
    branches:
      - '2'
jobs:
  build:
    name: build-docs
    runs-on: ubuntu-latest
    steps:
      - name: Run build hook
        run: curl -X POST -d {} ${{ secrets.NETLIFY_BUILD_HOOK }}